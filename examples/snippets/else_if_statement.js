function test() {
    if (a > 0) {
        //result = "positive";
        let result;
    } else if (a < -100) {
        //result = "less than -100";
        let result;
    } else {
        //result = "between -100 and 0 inclusive";
        let result;
    }
}